const React = require('react');
const styles = require('./styles');

const App = () => {
    return (
        <div className={styles['video-container']}>
            HI
        </div>
    );
};

module.exports = App;
